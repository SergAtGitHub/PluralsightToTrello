import { SafeProcessor } from 'solid-pipelines'
import { OnPopupClosedArguments } from './OnPopupClosedArguments';

export abstract class OnPopupClosedProcessor extends SafeProcessor<OnPopupClosedArguments> { }
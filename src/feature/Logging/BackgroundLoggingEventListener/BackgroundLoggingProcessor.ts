import { BackgroundLoggingArguments } from './BackgroundLoggingArguments'
import { SafeProcessor } from 'solid-pipelines'
import * as Processors from './processors'
import { BaseMessageListenerProcessor } from '../../../foundation';

export abstract class BackgroundLoggingProcessor extends BaseMessageListenerProcessor {
}